
//-----MULTIBAR CHART-----

//create function multibar() - param: selector, data, options

//create default configurations (width, height, max val)

//make initial axes

/* update function: updates after data source changes
    d.value = numpub or numpub_total
    d.name = Field if there, if not then domain, if not the group

    sort data by descending order

    get domain colors

    tooltip, x, y variables

    ENTER EXIT UPDATE FOR:
     -make labels 

     -create bars

    return update

*/

//call update

//---HTML SCRIPT---
//load function: loads in datasets and calls multibar function to draw
